json.extract! flashcard, :id, :data, :meaning, :reading, :kunreading, :onreading, :syn, :tos, :meaningmem, :readingmem, :srslevel, :cardtype, :user_id, :created_at, :updated_at
json.url flashcard_url(flashcard, format: :json)
