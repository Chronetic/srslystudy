json.extract! card, :id, :data, :meaning, :reading, :kunreading, :onreading, :syn, :tos, :meaningmem, :readingmem, :srslevel, :cardtype, :deck_id, :answer, :created_at, :updated_at
json.url card_url(card, format: :json)
